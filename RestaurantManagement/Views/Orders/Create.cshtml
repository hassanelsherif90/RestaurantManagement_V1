@model OrderCreateViewModel

@{
    SelectList selectListItems = new SelectList(Model.TableOptions, "MenuItemId", "Quantity");
}

<div class="container">
    <h2>إضافة طلب جديد</h2>
    <form asp-action="Create">
        <div class="form-group">
            <label asp-for="TableId" class="control-label"></label>
            <select asp-for="TableId" class="form-control" asp-items="selectListItems">
                <option value="0" selected> -- selectd Item ----</option>
            </select>
        </div>

        <button type="submit" class="btn btn-primary">إضافة</button>
        <a asp-action="Index" class="btn btn-secondary">إلغاء</a>
    </form>
</div>
